{
  "filename": "addDoc.md",
  "__html": "<h2>Adding a document</h2>\n<p>Once the document is done, it needs to be added to the page.</p>\n<h3>Where</h3>\n<p>According to the language version of the document, put <code>zh-cn</code> or <code>en-us</code> in the <code>docs</code> directory, which can be one or more level directory depth.</p>\n<h3>Menu Configuration</h3>\n<p>After the document is placed in the directory of the corresponding language, it needs to be configured in <code>site_config/docs.js</code>. The <code>link</code> field is used to configure the access address. Note that this path needs to start with a language (<code>/zh-cn</code> or <code>/en-us</code>).</p>\n<p>For example, if a document is stored in <code>/docs/zh-cn/demo/xxx.md</code>, the configuration value of the link field is <code>/zh-cn/docs/demo/xxx.html</code>.</p>\n"
}